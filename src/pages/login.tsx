import NavBar from "@/components/navbar";

const Home = () => {
  return (
    <div>
      <NavBar />
      <section>
        <button>Username</button>
        <button>Password</button>
        <button>Login</button>
      </section>
    </div>
  );
};
export default Home;
